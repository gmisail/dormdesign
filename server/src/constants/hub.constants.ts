export const DEBUG_MESSAGES = Boolean(process.env.DEBUG_MESSAGES ?? "false");
export const USE_DEBUGGER = true; // print contents of every room for every ping
export const PONG_TIME = 15 * 1000; // check every 15 seconds
